package com.keremcomert.javalib;

public interface Performance {

    /**
     * Since this is an interface, only the signatures of the methods are provided.
     * The implementations are left to the classes that implement this interface.
     * @return Float in both cases after calculations.
     */
    Float credit();
    Float monthlyFee();
}
